import 'package:flutter/material.dart';

class LocatorPage extends StatelessWidget {
  const LocatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("LocatorPage"),),
    );
  }
}