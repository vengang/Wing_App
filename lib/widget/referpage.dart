import 'package:flutter/material.dart';

class Referpage extends StatelessWidget {
  const Referpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Refer Page"),),
    );
  }
}